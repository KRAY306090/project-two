<div class="container mt-3">
    
        {{#each posts as |post|}}
        <div class="card bg-light mt-3">
            {{> post-info post }}
        </div>
        {{/each}}
   
</div>
<script src="/javascript/homepage.js"></script>